# ball-in-a-box

Build an interactive 3D demo of a ball perpetually bouncing inside of a transparent box. Allow the user to zoom, pan, and rotate the box in 3D
The entire demo shall be built with WebGL and run in a single HTML file.

I can't find the prompt for Ball in a Box WebGPU. It Loads a camera that bounces from the Kronos GLB sample folder.
